#!/usr/bin/env ruby

# Add the lib directory to the load path
$LOAD_PATH.unshift(File.join(File.dirname(__FILE__), '..', 'lib'))

require 'shiftcare'

# Run the application
ShiftCare::Application.new.run 